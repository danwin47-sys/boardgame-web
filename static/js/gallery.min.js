let allGames=[];let filteredGames=[];let activeFilters={players:new Set(),location:new Set(),status:'all'};document.addEventListener('DOMContentLoaded',()=>{loadGames();});async function loadGames(){try{const response=await fetch('/api/gallery/games');const data=await response.json();if(data.success){allGames=data.games;document.getElementById('totalGamesCount').textContent=`共 ${data.total}款桌遊`;applyFilters();renderGames();}else{showError('載入遊戲列表失敗: '+(data.error||'未知錯誤'));}}catch(error){console.error('載入遊戲失敗:',error);showError('無法連接到伺服器，請稍後再試');}}
function toggleFilter(type,value){const filterSet=activeFilters[type];if(filterSet.has(value)){filterSet.delete(value);}else{filterSet.add(value);}
updateFilterButtonStyles();applyFilters();renderGames();}
function toggleStatusFilter(status){activeFilters.status=status;document.querySelectorAll('[data-filter-type="status"]').forEach(btn=>{if(btn.dataset.value===status){btn.classList.add('active');}else{btn.classList.remove('active');}});applyFilters();renderGames();}
function updateFilterButtonStyles(){document.querySelectorAll('.filter-tag:not([data-filter-type="status"])').forEach(btn=>{const type=btn.dataset.filterType;const value=btn.dataset.value;let actualValue=value;if(type==='players'){actualValue=parseInt(value);}
if(activeFilters[type]&&activeFilters[type].has(actualValue)){btn.classList.add('active');}else{btn.classList.remove('active');}});}
function clearAllFilters(){activeFilters={players:new Set(),location:new Set(),status:'all'};document.querySelectorAll('.filter-tag').forEach(btn=>{btn.classList.remove('active');});document.querySelector('[data-filter-type="status"][data-value="all"]').classList.add('active');applyFilters();renderGames();}
function applyFilters(){filteredGames=allGames.filter(game=>{if(activeFilters.status!=='all'){if(game.status!==activeFilters.status){return false;}}
if(activeFilters.players.size>0){const matchPlayers=[...activeFilters.players].every(p=>{const min=game.minPlayers||1;const max=game.maxPlayers||10;if(p==='10+'){return max>=10;}
return p>=min&&p<=max;});if(!matchPlayers)return false;}
if(activeFilters.location.size>0){const matchLocation=[...activeFilters.location].every(loc=>game.location===loc);if(!matchLocation)return false;}
return true;});const filterCount=document.getElementById('filteredGamesCount');if(filteredGames.length<allGames.length){filterCount.textContent=`(已篩選出 ${filteredGames.length}款)`;filterCount.style.color='#ffd43b';}else{filterCount.textContent='';}}
function renderGames(){const grid=document.getElementById('galleryGrid');if(filteredGames.length===0){grid.innerHTML=`<div class="no-results"><h3>😔 沒有找到符合條件的桌遊</h3><p>請嘗試調整篩選條件</p></div>`;return;}
grid.innerHTML=filteredGames.map(game=>{const imageUrl=getGameImage(game);const isBorrowed=game.status==='借出';const statusClass=isBorrowed?'borrowed':'available';const statusText=isBorrowed?'借出':'在庫';return`<div class="game-card"onclick="showGameDetail('${escapeHtml(game.id)}')"><div class="game-card-image"><img src="${imageUrl}"
alt="${escapeHtml(game.name)}"
loading="lazy"
onerror="this.src='https://via.placeholder.com/300x420/667eea/ffffff?text=No+Image'"><div class="game-top-labels">${game.label?`<span class="game-label">${escapeHtml(game.label)}</span>`:''}
${(game.minPlayers&&game.maxPlayers)?`<span class="game-players-badge">${game.minPlayers==game.maxPlayers?game.minPlayers+'人':game.minPlayers+'-'+game.maxPlayers+'人'}</span>`:''}</div><span class="game-status-badge ${statusClass}">${statusText}</span><div class="game-card-title">${escapeHtml(game.name)}</div></div></div>`;}).join('');}
function getGameImage(game){if(game.image){return game.image;}
if(game.thumbnail){return game.thumbnail;}
if(game.bggId){return`https:}
return'https://via.placeholder.com/300x420/667eea/ffffff?text=No+Image';}
function showGameDetail(gameId){const game=allGames.find(g=>g.id===gameId);if(!game){console.error('找不到遊戲:',gameId);return;}
const modal=document.getElementById('gameModal');const content=document.getElementById('modalContent');const imageUrl=getGameImage(game);content.innerHTML=`<button class="modal-close"onclick="closeModal()">✕</button><img src="${imageUrl}"
alt="${escapeHtml(game.name)}"
class="modal-image"
onerror="this.src='https://via.placeholder.com/600x840/667eea/ffffff?text=No+Image'"><h2>${escapeHtml(game.name)}</h2><div class="game-info">${game.minPlayers&&game.maxPlayers?`<p><strong>🎮 遊玩人數：</strong>${game.minPlayers}${game.minPlayers!==game.maxPlayers?`-${game.maxPlayers}`:''}人</p>`:''}
${game.minMinutes&&game.maxMinutes?`<p><strong>⏱️ 遊戲時間：</strong>${game.minMinutes}${game.minMinutes!==game.maxMinutes?`-${game.maxMinutes}`:''}分鐘</p>`:''}
${game.difficulty?`<p><strong>🎯 難度：</strong>${escapeHtml(game.difficulty)}</p>`:''}
${game.status?`<p><strong>📦 狀態：</strong>${escapeHtml(game.status)}</p>`:''}
${game.borrower?`<p><strong>👤 借閱人：</strong>${escapeHtml(game.borrower)}</p>`:''}
${game.location?`<p><strong>📍 位置：</strong>${escapeHtml(game.location)}</p>`:''}
${game.custodian?`<p><strong>🔑 保管人：</strong>${escapeHtml(game.custodian)}</p>`:''}
${game.types&&game.types.length>0?`<p><strong>🏷️ 類型：</strong>${game.types.map(t=>escapeHtml(t)).join(', ')}</p>`:''}
${game.tags&&game.tags.length>0?`<p><strong>🔖 標籤：</strong>${game.tags.map(t=>escapeHtml(t)).join(', ')}</p>`:''}</div>${game.bggId?`<a href="https://boardgamegeek.com/boardgame/${game.bggId}"
target="_blank"
rel="noopener noreferrer"
class="bgg-link">在 BoardGameGeek 查看 ↗</a>`:''}`;modal.classList.add('active');document.body.style.overflow='hidden';}
function closeModal(){const modal=document.getElementById('gameModal');modal.classList.remove('active');document.body.style.overflow='';}
document.addEventListener('keydown',(e)=>{if(e.key==='Escape'){closeModal();}});function escapeHtml(text){const div=document.createElement('div');div.textContent=text;return div.innerHTML;}
function showError(message){const grid=document.getElementById('galleryGrid');grid.innerHTML=`<div class="no-results"><h3>⚠️ 發生錯誤</h3><p>${escapeHtml(message)}</p></div>`;}