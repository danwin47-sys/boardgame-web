# 🚀 Boardgame-Web 效能優化計畫

**任務 ID**: performance_optimization_2025_12_21  
**建立時間**: 2025-12-21 15:20  
**目標**: 全面優化系統效能，提升使用者體驗和系統響應速度

---

## 📊 一、效能分析結果

### 1.1 後端效能瓶頸

#### 🔴 高優先級問題

1. **缺乏 Redis/Memcached 等專業快取系統**
   - 現狀：使用簡單的記憶體快取 (`SimpleCache`)
   - 問題：多進程環境下快取不共享，重啟後快取全部失效
   - 影響：BGG API 呼叫頻繁，Google Sheets 讀取重複

2. **Google Sheets API 呼叫未批次化**
   - 現狀：`sheets_client.py` 中的更新操作使用單一呼叫
   - 問題：多次更新需要多次 API 請求
   - 影響：批次操作（如批次借出）效能差

3. **BGG API 無並發控制**
   - 現狀：`bgg_service.py` 中的 API 呼叫是同步的
   - 問題：獲取多個遊戲資訊時串行執行
   - 影響：推薦頁面載入緩慢

4. **缺乏資料庫索引和查詢優化**
   - 現狀：BGG Ranks 使用 SQLite，但未檢查索引
   - 問題：可能缺少必要的索引
   - 影響：排名查詢可能變慢

#### 🟡 中優先級問題

5. **前端資源未壓縮和合併**
   - 現狀：CSS/JS 檔案分散，未壓縮
   - 問題：HTTP 請求數多，檔案大小未優化
   - 影響：首次載入速度慢

6. **圖片未使用 CDN 或快取策略**
   - 現狀：BGG 圖片直接從 BGG 載入
   - 問題：外部資源載入不穩定
   - 影響：圖片載入可能很慢

7. **缺乏 HTTP 快取標頭**
   - 現狀：靜態資源未設置適當的快取標頭
   - 問題：瀏覽器無法有效快取資源
   - 影響：重複訪問仍需重新下載資源

### 1.2 前端效能瓶頸

#### 🔴 高優先級問題

8. **大型表格渲染效能差**
   - 現狀：`script.js` 中 `renderTable()` 一次渲染所有遊戲
   - 問題：遊戲數量多時 DOM 操作過多
   - 影響：頁面卡頓，渲染時間長

9. **搜尋防抖時間過短**
   - 現狀：防抖設置為 50ms
   - 問題：仍然會觸發過多的搜尋操作
   - 影響：輸入時可能卡頓

#### 🟡 中優先級問題

10. **未使用虛擬滾動**
    - 現狀：所有遊戲同時渲染到 DOM
    - 問題：大量 DOM 節點影響效能
    - 影響：滾動可能不流暢

11. **圖片未使用懶載入**
    - 現狀：所有遊戲縮圖同時載入
    - 問題：初始載入時間長
    - 影響：首屏渲染慢

---

## 🎯 二、優化策略

### 階段一：後端快取優化（高優先級）

#### 2.1 引入 Redis 快取層

**目標**: 使用 Redis 作為分散式快取，取代記憶體快取

**實施步驟**:
1. 安裝 Redis 和 Python 客戶端 (`redis`, `flask-caching`)
2. 建立 `core/redis_cache.py` 包裝 Redis 操作
3. 更新 `cache.py` 支援 Redis 後端
4. 配置快取策略：
   - BGG 遊戲詳情：24 小時
   - Google Sheets 資料：5 分鐘
   - BGG 推薦列表：1 小時

**預期效果**: 
- 減少 80% 的 BGG API 呼叫
- 減少 60% 的 Google Sheets 讀取
- 支援多進程部署

#### 2.2 優化 Google Sheets 批次操作

**目標**: 使用 `batch_update` API 減少請求次數

**實施步驟**:
1. 重構 `sheets_client.py` 的更新方法
2. 實作批次更新佇列機制
3. 合併多個更新操作為單一 API 呼叫

**預期效果**:
- 批次操作速度提升 5-10 倍
- 減少 API 配額消耗

#### 2.3 BGG API 並發請求

**目標**: 使用非同步請求並行獲取多個遊戲資訊

**實施步驟**:
1. 安裝 `aiohttp` 或使用 `concurrent.futures`
2. 重構 `bgg_api_client.py` 支援並發
3. 實作請求速率限制（避免被 BGG 封鎖）

**預期效果**:
- 推薦頁面載入速度提升 3-5 倍
- 批次查詢效能大幅提升

### 階段二：前端渲染優化（高優先級）

#### 2.4 實作虛擬滾動或分頁

**目標**: 只渲染可見區域的遊戲，減少 DOM 節點

**實施步驟**:
1. 選擇方案：
   - 方案 A：使用虛擬滾動庫（如 `virtual-scroller`）
   - 方案 B：實作簡單的分頁機制（每頁 50 筆）
2. 更新 `script.js` 的 `renderTable()` 函數
3. 保持搜尋和篩選功能正常運作

**預期效果**:
- 初始渲染時間減少 70%
- 滾動流暢度大幅提升

#### 2.5 圖片懶載入

**目標**: 只載入可見區域的圖片

**實施步驟**:
1. 使用原生 `loading="lazy"` 屬性
2. 或使用 Intersection Observer API
3. 添加佔位圖片（placeholder）

**預期效果**:
- 首屏載入時間減少 50%
- 減少不必要的網路請求

### 階段三：資源優化（中優先級）

#### 2.6 前端資源壓縮和合併

**目標**: 減少 HTTP 請求數和檔案大小

**實施步驟**:
1. 使用 Webpack 或 Vite 打包前端資源
2. 啟用 CSS/JS 壓縮
3. 實作程式碼分割（code splitting）

**預期效果**:
- 首次載入時間減少 30-40%
- 減少 HTTP 請求數

#### 2.7 設置 HTTP 快取標頭

**目標**: 讓瀏覽器有效快取靜態資源

**實施步驟**:
1. 在 Flask 中設置靜態資源快取標頭
2. 使用版本號或 hash 實作快取破壞（cache busting）
3. 設置適當的 `Cache-Control` 和 `ETag`

**預期效果**:
- 重複訪問速度提升 80%
- 減少伺服器負載

#### 2.8 BGG 圖片代理和快取

**目標**: 本地快取 BGG 圖片，提升載入速度

**實施步驟**:
1. 建立圖片代理端點 `/api/image-proxy`
2. 使用 Redis 或檔案系統快取圖片
3. 設置適當的快取過期時間

**預期效果**:
- 圖片載入速度提升 2-3 倍
- 減少對 BGG 伺服器的依賴

### 階段四：資料庫優化（中優先級）

#### 2.9 SQLite 索引優化

**目標**: 確保 BGG Ranks 資料庫有適當的索引

**實施步驟**:
1. 分析 `bgg_ranks_service.py` 的查詢模式
2. 添加必要的索引（如 `bgg_id`, `rank`）
3. 使用 `EXPLAIN QUERY PLAN` 驗證查詢效能

**預期效果**:
- 排名查詢速度提升 5-10 倍

#### 2.10 考慮升級到 PostgreSQL

**目標**: 如果資料量持續增長，考慮使用更強大的資料庫

**實施步驟**:
1. 評估資料量和查詢複雜度
2. 如有必要，遷移到 PostgreSQL
3. 使用連線池（如 `psycopg2-pool`）

**預期效果**:
- 支援更大的資料量
- 更好的並發效能

---

## 📋 三、實施優先順序

### 🚀 第一波（立即實施）- 預計 2-3 天

1. ✅ **引入 Redis 快取** (2.1)
2. ✅ **實作虛擬滾動/分頁** (2.4)
3. ✅ **圖片懶載入** (2.5)
4. ✅ **優化搜尋防抖** (調整為 200-300ms)

**預期總體提升**: 首屏載入速度提升 60%，操作響應速度提升 50%

### 🔧 第二波（後續優化）- 預計 3-4 天

5. ✅ **Google Sheets 批次操作** (2.2)
6. ✅ **BGG API 並發請求** (2.3)
7. ✅ **HTTP 快取標頭** (2.7)
8. ✅ **SQLite 索引優化** (2.9)

**預期總體提升**: 批次操作速度提升 5 倍，API 響應速度提升 3 倍

### 🎨 第三波（進階優化）- 預計 2-3 天

9. ✅ **前端資源壓縮** (2.6)
10. ✅ **BGG 圖片代理** (2.8)

**預期總體提升**: 整體載入速度再提升 30%

---

## 🧪 四、測試計畫

### 4.1 效能基準測試

**測試項目**:
1. 首頁載入時間（100 筆遊戲）
2. 首頁載入時間（500 筆遊戲）
3. 搜尋響應時間
4. 批次借出操作時間（5 筆）
5. 推薦頁面載入時間

**測試工具**:
- Chrome DevTools (Performance, Network)
- Lighthouse
- `pytest-benchmark` (後端)

### 4.2 負載測試

**測試項目**:
1. 並發使用者測試（10/50/100 使用者）
2. API 端點壓力測試

**測試工具**:
- `locust` 或 `ab` (Apache Bench)

### 4.3 回歸測試

**確保優化不破壞現有功能**:
1. 執行完整的 `pytest` 測試套件
2. 手動測試關鍵流程
3. 驗證資料一致性

---

## 📈 五、成功指標

### 5.1 效能指標

| 指標 | 優化前 | 目標 | 測量方式 |
|------|--------|------|----------|
| 首頁載入時間（100 筆） | 待測 | < 1 秒 | Lighthouse |
| 首頁載入時間（500 筆） | 待測 | < 2 秒 | Lighthouse |
| 搜尋響應時間 | 待測 | < 100ms | Chrome DevTools |
| 批次借出（5 筆） | 待測 | < 2 秒 | 手動計時 |
| 推薦頁面載入 | 待測 | < 3 秒 | Lighthouse |
| Lighthouse 效能分數 | 待測 | > 90 | Lighthouse |

### 5.2 資源使用指標

| 指標 | 優化前 | 目標 |
|------|--------|------|
| BGG API 呼叫次數 | 待測 | 減少 80% |
| Google Sheets 讀取次數 | 待測 | 減少 60% |
| 首頁 DOM 節點數 | 待測 | < 500 |
| 記憶體使用量 | 待測 | 減少 30% |

---

## 🛠️ 六、技術債務清理

### 6.1 程式碼重構

1. **統一快取介面**: 建立統一的快取抽象層
2. **服務層解耦**: 減少服務之間的直接依賴
3. **錯誤處理增強**: 添加更完善的錯誤處理和重試機制

### 6.2 文檔更新

1. 更新 `README.md` 添加 Redis 安裝說明
2. 建立效能優化文檔
3. 更新部署指南

---

## 📝 七、風險評估

### 7.1 技術風險

| 風險 | 影響 | 機率 | 緩解措施 |
|------|------|------|----------|
| Redis 依賴增加部署複雜度 | 中 | 高 | 提供 Docker Compose 配置，支援降級到記憶體快取 |
| 虛擬滾動破壞現有功能 | 高 | 中 | 充分測試，保留分頁備選方案 |
| BGG API 並發被封鎖 | 中 | 低 | 實作速率限制和重試機制 |
| 快取不一致問題 | 高 | 中 | 實作快取失效策略，添加手動刷新功能 |

### 7.2 業務風險

| 風險 | 影響 | 機率 | 緩解措施 |
|------|------|------|----------|
| 優化期間服務中斷 | 高 | 低 | 使用功能開關，漸進式部署 |
| 使用者習慣改變 | 中 | 中 | 保持 UI 一致性，提供使用說明 |

---

## ✅ 八、驗收標準

### 8.1 必須達成

- [ ] 所有現有測試通過
- [ ] 首頁載入時間 < 2 秒（500 筆遊戲）
- [ ] Lighthouse 效能分數 > 85
- [ ] 無新增的嚴重 bug

### 8.2 期望達成

- [ ] Lighthouse 效能分數 > 90
- [ ] 批次操作速度提升 5 倍以上
- [ ] BGG API 呼叫減少 80%
- [ ] 使用者體驗明顯提升（主觀評估）

---

## 📅 九、時間規劃

| 階段 | 工作內容 | 預計時間 | 負責人 |
|------|----------|----------|--------|
| 第一波 | Redis + 前端渲染優化 | 2-3 天 | AI Agent |
| 第二波 | 批次操作 + 並發優化 | 3-4 天 | AI Agent |
| 第三波 | 資源優化 + 圖片代理 | 2-3 天 | AI Agent |
| 測試驗證 | 效能測試 + 回歸測試 | 1-2 天 | AI Agent |
| **總計** | | **8-12 天** | |

---

## 🎯 十、下一步行動

### 立即開始（第一波優化）

1. **安裝 Redis 和相關套件**
   ```bash
   pip install redis flask-caching
   ```

2. **建立 Redis 快取服務**
   - 建立 `core/redis_cache.py`
   - 更新 `core/cache.py` 支援 Redis

3. **實作前端分頁機制**
   - 修改 `static/js/script.js`
   - 每頁顯示 50 筆遊戲

4. **添加圖片懶載入**
   - 使用 `loading="lazy"` 屬性

5. **執行基準測試**
   - 記錄優化前的效能數據

---

**計畫建立者**: Boardgame-Web Expert (AI Agent)  
**最後更新**: 2025-12-21 15:20  
**狀態**: 待審核 ✋
